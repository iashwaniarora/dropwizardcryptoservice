package com.appleassignment.configuration;


import io.dropwizard.Configuration;

/**
 * Configuration Class which can be used to read from the property file.
 */
public class CryptoServiceConfiguration extends Configuration {
   /** This class can be used to read configuration from the properties file.
    * As of now there are no configurations but certain configuration related to
    * database
    * secure key like seed,length
    * logging level
    * can be used in the configuration properties.
    */
}